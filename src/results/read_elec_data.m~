clear variables
close all
home

times = importdata('electrode.times');

filename = 'electrode_7_300.data';

fileID = fopen(filename);
C = textscan(fileID,'%s %s %f32 %d8 %u %f %f %s %f');
fclose(fileID);
